# rogue-entrance
Beat id: 'sector-8-entrance' · 5 panel(s)
Source: src/content/beats.ts (grep "export const rogueEntranceBeat")

**Plays when:** `App.tsx`'s `enterMissionWithTransitionCheck`, on first entry to Sector 8 — looked up via `sectorBeats[8]` (see sector-beats.md) — before the player reaches m8-1. Not independently replayable.

**Arc/tone:** 5 panels, ROGUE.exe's first direct on-screen appearance, escalating through its `entrance1`–`entrance4` sprite states from mocking ("cute") to a direct challenge ("Sector 8. My house, my rules"). Fulfils GAME_DESIGN_BRIEF.md's reserved first-appearance voice slot.
