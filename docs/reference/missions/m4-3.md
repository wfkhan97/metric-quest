# m4-3 — Analyst sandbox
Sector 4 · Temporary tables, multi-statement SQL · 40 pts
Source: src/lib/missions.ts (grep "id: 'm4-3'") · Diagnostics: src/lib/diagnostics.ts (grep "'m4-3':")

**Common mistakes:**
- Missing the $10 filter
- Sorted ascending instead of descending

**Key insight:** Workbench proven. The temp table exists only for this run — it is gone the moment the terminal resets — but while it is alive, it queries exactly like a real table, which is the whole point of staging work before the final analysis.
