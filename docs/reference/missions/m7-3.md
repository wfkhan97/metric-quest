# m7-3 — Every market mention
Sector 7 · UNION ALL keeps duplicates · 30 pts
Source: src/lib/missions.ts (grep "id: 'm7-3'") · Diagnostics: src/lib/diagnostics.ts (grep "'m7-3':")

**Common mistakes:**
- UNION instead of UNION ALL

**Key insight:** 471 mentions: 59 customer rows plus 412 invoice rows, every one kept. That's a completely different question from the 24 distinct markets UNION found earlier — UNION ALL is the tool whenever the count of duplicates is itself the signal, not noise to be scrubbe…
