# m1-6 — Flagged markets
Sector 1 · IN for matching against a list · 20 pts
Source: src/lib/missions.ts (grep "id: 'm1-6'") · Diagnostics: src/lib/diagnostics.ts (grep "'m1-6':")

**Common mistakes:**
- Missing one of the three markets
- Not sorted by country first

**Key insight:** Shortlist pulled. WHERE BillingCountry IN (...) is exactly WHERE BillingCountry = 'Chile' OR BillingCountry = 'Hungary' OR BillingCountry = 'Norway', just written as one list instead of a chain of ORs that gets uglier every time the shortlist grows.
