# m4-2 — Revenue leaderboard
Sector 4 · CTE · 35 pts
Source: src/lib/missions.ts (grep "id: 'm4-2'") · Diagnostics: src/lib/diagnostics.ts (grep "'m4-2':")

**Common mistakes:**
- Missing LIMIT 10
- Sorted ascending instead of descending

**Key insight:** Leaderboard staged. A CTE computes LifetimeRevenue exactly once, so every join and sort downstream reads the same staged number instead of ROGUE.exe recomputing (and drifting) it on the fly.
