# m7-4 — Markets that stayed active
Sector 7 · INTERSECT · 35 pts
Source: src/lib/missions.ts (grep "id: 'm7-4'") · Diagnostics: src/lib/diagnostics.ts (grep "'m7-4':")

**Common mistakes:**
- UNION instead of INTERSECT
- Missing the 2011 filter

**Key insight:** 21 markets confirmed active in both directions. INTERSECT is stricter than UNION — UNION would have combined both lists regardless of overlap, but INTERSECT only keeps what's genuinely true on both sides at once, which is exactly what a renewal decision needs.
