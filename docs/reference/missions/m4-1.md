# m4-1 — Above-average invoices
Sector 4 · Scalar subquery · 35 pts
Source: src/lib/missions.ts (grep "id: 'm4-1'") · Diagnostics: src/lib/diagnostics.ts (grep "'m4-1':")

**Common mistakes:**
- Missing the AVG subquery
- Sorted ascending instead of descending

**Key insight:** Triage restored. The subquery computes one number — the real average — before the outer WHERE ever runs, so every invoice gets compared to the same real bar instead of nothing at all.
