# m2-3 — Customer base sanity check
Sector 2 · COUNT(*), COUNT(column), COUNT(DISTINCT column) · 25 pts
Source: src/lib/missions.ts (grep "id: 'm2-3'") · Diagnostics: src/lib/diagnostics.ts (grep "'m2-3':")

**Common mistakes:**
- Missing DISTINCT

**Key insight:** Corruption isolated. All three numbers pull from the same table, but only DISTINCT collapses repeat buyers to one each — the gap between 412 and 59 is exactly the shape of overcounting ROGUE.exe relies on elsewhere.
