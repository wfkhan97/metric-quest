# m5-1 — Sales by year
Sector 5 · SQLite dates, strftime · 35 pts
Source: src/lib/missions.ts (grep "id: 'm5-1'") · Diagnostics: src/lib/diagnostics.ts (grep "'m5-1':")

**Common mistakes:**
- Missing strftime on InvoiceDate
- Missing GROUP BY

**Key insight:** Gears unstuck. strftime('%Y', ...) reads the real year out of every invoice date, so revenue lines up on an actual five-year timeline instead of the one jammed year ROGUE.exe was stalling on.
