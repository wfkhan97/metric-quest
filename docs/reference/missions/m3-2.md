# m3-2 — What did the customer buy?
Sector 3 · Multi-table joins and aliases · 30 pts
Source: src/lib/missions.ts (grep "id: 'm3-2'") · Diagnostics: src/lib/diagnostics.ts (grep "'m3-2':")

**Common mistakes:**
- Missing JOIN
- Missing the invoice filter

**Key insight:** Line items restored. InvoiceLine is the join table connecting a specific invoice to the specific tracks bought on it — without it, an invoice is just a total with no receipt behind it.
