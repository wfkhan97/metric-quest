# m1-5 — Mid-tier audit window
Sector 1 · BETWEEN for range filters · 20 pts
Source: src/lib/missions.ts (grep "id: 'm1-5'") · Diagnostics: src/lib/diagnostics.ts (grep "'m1-5':")

**Common mistakes:**
- Incomplete range
- Sorted descending instead of ascending

**Key insight:** Gap swept. BETWEEN 15 AND 20 is shorthand for Total >= 15 AND Total <= 20, but written as one inclusive range instead of two conditions to keep straight — exactly the band a scan built around round thresholds would have skipped.
