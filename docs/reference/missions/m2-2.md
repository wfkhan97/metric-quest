# m2-2 — Market qualification
Sector 2 · HAVING vs. WHERE · 25 pts
Source: src/lib/missions.ts (grep "id: 'm2-2'") · Diagnostics: src/lib/diagnostics.ts (grep "'m2-2':")

**Common mistakes:**
- Missing HAVING

**Key insight:** Filter rewired. WHERE runs before grouping, so it can't see a summed total that doesn't exist yet — HAVING runs after GROUP BY, which is exactly why it's the only clause that can filter on SUM(Total).
