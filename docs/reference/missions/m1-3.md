# m1-3 — Search the catalog
Sector 1 · LIKE and LOWER for text search · 20 pts
Source: src/lib/missions.ts (grep "id: 'm1-3'") · Diagnostics: src/lib/diagnostics.ts (grep "'m1-3':")

**Common mistakes:**
- Missing the % wildcards

**Key insight:** Search terminal fixed. LOWER() normalizes case before the pattern match runs, so ROGUE.exe's case trick can't hide a match just by capitalizing it differently.
