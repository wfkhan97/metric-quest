# m1-4 — Price per minute
Sector 1 · Calculations, aliases, ROUND · 25 pts
Source: src/lib/missions.ts (grep "id: 'm1-4'") · Diagnostics: src/lib/diagnostics.ts (grep "'m1-4':")

**Common mistakes:**
- Missing the price filter
- Integer division on the minute conversion

**Key insight:** Terminal restored. Dividing by 60000.0 keeps this a real fraction instead of integer-dividing it away — that decimal is what exposes tracks charging a premium price for barely any runtime.
