# m7-1 — Unified customer markets
Sector 7 · UNION vs. UNION ALL · 30 pts
Source: src/lib/missions.ts (grep "id: 'm7-1'") · Diagnostics: src/lib/diagnostics.ts (grep "'m7-1':")

**Common mistakes:**
- UNION ALL instead of UNION

**Key insight:** Vault deduplicated. UNION collapses any country appearing on both sides down to one row — UNION ALL would have kept every one of ROGUE.exe's duplicates intact.
