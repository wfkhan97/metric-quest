# m7-2 — Reusable revenue view
Sector 7 · Views, reusable analysis · 40 pts
Source: src/lib/missions.ts (grep "id: 'm7-2'") · Diagnostics: src/lib/diagnostics.ts (grep "'m7-2':")

**Common mistakes:**
- Missing LIMIT 5
- Sorted ascending instead of descending

**Key insight:** View restored. CountryRevenue now exists as a saved query for this run — nobody has to rewrite the GROUP BY from scratch, which is exactly the shared asset ROGUE.exe deleted.
