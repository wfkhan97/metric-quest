# m1-2 — Market map
Sector 1 · DISTINCT · 15 pts
Source: src/lib/missions.ts (grep "id: 'm1-2'") · Diagnostics: src/lib/diagnostics.ts (grep "'m1-2':")

**Common mistakes:**
- Missing DISTINCT

**Key insight:** Map restored. DISTINCT collapsed ROGUE.exe's duplicate entries back down to one row per country — 24 real markets, no padding.
