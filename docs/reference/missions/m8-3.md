# m8-3 — The cut list
Sector 8 · JOIN, aggregation, and HAVING on the low end · 40 pts
Source: src/lib/missions.ts (grep "id: 'm8-3'") · Diagnostics: src/lib/diagnostics.ts (grep "'m8-3':")

**Common mistakes:**
- Missing HAVING
- Sorted descending instead of ascending

**Key insight:** Cut list exposed. Revenue alone put eleven genres on ROGUE.exe's chopping block — but this table has no margin, no licensing cost, and no customer-retention data in it.
