# m9-1 — Frame the real question
Sector 9 · Scalar subquery for a computed date scope · 20 pts
Source: src/lib/missions.ts (grep "id: 'm9-1'") · Diagnostics: src/lib/diagnostics.ts (grep "'m9-1':")

**Common mistakes:**
- Missing the MAX(year) subquery
- Sorted ascending instead of descending

**Key insight:** Question framed. The subquery pulls whatever year is actually most recent instead of a guess baked into the query — the same discipline that keeps a real analyst from running on a stale assumption.
