# m5-3 — Ledger lifespan
Sector 5 · Date arithmetic with strftime · 30 pts
Source: src/lib/missions.ts (grep "id: 'm5-3'") · Diagnostics: src/lib/diagnostics.ts (grep "'m5-3':")

**Common mistakes:**
- Missing strftime('%J', ...)
- MIN and MAX swapped

**Key insight:** Vault age confirmed: 1,816 days, not "just started." strftime('%J', ...) turns each date into a comparable day-number, and subtracting two of them is exactly how you'd measure any duration — signup-to-churn, order-to-delivery, first-sale-to-most-recent — the s…
