# m1-1 — Priority invoices
Sector 1 · Filter, sort, and limit · 20 pts
Source: src/lib/missions.ts (grep "id: 'm1-1'") · Diagnostics: src/lib/diagnostics.ts (grep "'m1-1':")

**Common mistakes:**
- Missing the USA filter
- Missing LIMIT
- Sorted ascending instead of descending

**Key insight:** Terminal restored. Filtering BillingCountry before sorting is what kept ROGUE.exe's junk rows out of the ranking — sort first and you'd be ranking noise instead of real invoices.
