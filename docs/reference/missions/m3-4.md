# m3-4 — Customers missing from this year's sales
Sector 3 · LEFT JOIN and NULL checks · 30 pts
Source: src/lib/missions.ts (grep "id: 'm3-4'") · Diagnostics: src/lib/diagnostics.ts (grep "'m3-4':")

**Common mistakes:**
- JOIN instead of LEFT JOIN
- Missing the IS NULL check

**Key insight:** Reactivation list restored. An INNER JOIN drops any customer with zero matching invoices — exactly the people this list needs.
