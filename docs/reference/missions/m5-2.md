# m5-2 — Month-end momentum
Sector 5 · Dates and grouping by a derived value · 35 pts
Source: src/lib/missions.ts (grep "id: 'm5-2'") · Diagnostics: src/lib/diagnostics.ts (grep "'m5-2':")

**Common mistakes:**
- Grouped by month without year
- Missing LIMIT 10

**Key insight:** Dial freed. %Y-%m keeps months sortable across years instead of colliding every January — that is what let the real top-10 months surface instead of the one frozen reading ROGUE.exe was stuck on.
