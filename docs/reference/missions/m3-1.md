# m3-1 — Name the high-value customers
Sector 3 · INNER JOIN and foreign keys · 30 pts
Source: src/lib/missions.ts (grep "id: 'm3-1'") · Diagnostics: src/lib/diagnostics.ts (grep "'m3-1':")

**Common mistakes:**
- Missing JOIN
- Missing the USA filter

**Key insight:** Relay reconnected. The CustomerId foreign key is the wire ROGUE.exe cut — joining on it turns anonymous invoice rows back into a named follow-up list the sales lead can actually use.
