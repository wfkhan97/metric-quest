# m6-1 — Invoice tiers
Sector 6 · CASE · 35 pts
Source: src/lib/missions.ts (grep "id: 'm6-1'") · Diagnostics: src/lib/diagnostics.ts (grep "'m6-1':")

**Common mistakes:**
- Missing ELSE
- CASE conditions checked in the wrong order

**Key insight:** Switch rebuilt. CASE routes every invoice into exactly one labeled bin by testing conditions in order, top to bottom — the same three-way sort ROGUE.exe had jammed into a single lane.
