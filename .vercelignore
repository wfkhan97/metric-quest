# The optional Moonshot AI tutor remains out of the release candidate.
# Excluding api/ prevents any tutor route from being deployed while the UI
# is separately gated in MissionView (AI_TUTOR_ENABLED). Remove this line
# only after item 3's model-selection, security, and release gates pass and
# MOONSHOT_API_KEY has been configured as a Vercel Sensitive variable.
api
