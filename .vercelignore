# Re-excluded 2026-08-20: deploying api/ made /api/chat crash on every
# request (FUNCTION_INVOCATION_FAILED even on a bare GET — an
# import/bundling failure, not application logic). AI_TUTOR_ENABLED is off
# in src/components/MissionView.tsx to match. See docs/BACKLOG.md item 3.
# Root cause not yet diagnosed; do not remove this line until a live
# request to /api/chat succeeds against a real deployment.
api
